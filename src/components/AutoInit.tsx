'use client';

// Auto-initialization is DISABLED
// Database should be managed manually via Neon dashboard or psql
// To re-enable: restore the original component code
export default function AutoInit() {
  return null;
}