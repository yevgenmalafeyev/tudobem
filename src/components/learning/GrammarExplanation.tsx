// This component is kept for backward compatibility but returns null
// since hints are now simple strings rather than structured grammar rules
export default function GrammarExplanation() {
  return null;
}